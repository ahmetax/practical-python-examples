[tool.poetry]
name = "poetry-package-management"
version = "1.0.0"
description = "Modern dependency and virtual environment management using Poetry"
authors = ["Ahmet Aksoy <ahmet@example.com>"]
readme = "README.md"
packages = [{include = "src"}]

[tool.poetry.dependencies]
python = "^3.12"
requests = "^2.31.0"
rich = "^13.7.1"

[tool.poetry.group.dev.dependencies]
pytest = "^8.1.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"